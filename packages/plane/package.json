{
  "name": "@agent-control-plane/plane",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @agent-control-plane/core build && tsc -p tsconfig.json",
    "check": "pnpm typecheck && pnpm test",
    "lint": "pnpm typecheck",
    "test": "vitest run",
    "typecheck": "pnpm --filter @agent-control-plane/core build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-control-plane/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}
